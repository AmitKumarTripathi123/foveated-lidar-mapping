# Global Model Selection Configuration

primary_model:
  name: "spvcnn"
  config_path: "configs/spvcnn.yaml"
  pretrained: true
  checkpoint: "checkpoints/spvcnn_pretrained.pt"

legacy_baseline_models:
  - name: "pointnet2_legacy"
    config_path: "configs/phase2.yaml"
    description: "PointNet++ set abstraction baseline model"
